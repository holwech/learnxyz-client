## LearnXYZ client
Created with React/Redux

### How it install
1. Install NodeJS and NPM
2. Clone repo
3. Go to repo-folder and run `npm install`
4. Possibly run `npm install babel-core` if error occurs on step 3
5. Run `npm run dev` to start server
